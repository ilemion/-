# 权限管理设计方案

## 1. 权限模型

### 1.1 RBAC模型设计

采用基于角色的访问控制(Role-Based Access Control)模型:

```
用户 → 角色 → 权限
```

**核心概念:**
- **用户(User):** 系统使用者
- **角色(Role):** 权限的集合
- **权限(Permission):** 对资源的操作权限
- **资源(Resource):** 系统功能模块或数据

### 1.2 权限层级

```
系统级权限
    ↓
模块级权限
    ↓
功能级权限
    ↓
数据级权限
    ↓
操作级权限
```

## 2. 数据库设计

### 2.1 权限表结构

```sql
-- 用户表
CREATE TABLE sys_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(100) NOT NULL COMMENT '密码(加密)',
    real_name VARCHAR(50) COMMENT '真实姓名',
    phone VARCHAR(20) COMMENT '手机号',
    email VARCHAR(100) COMMENT '邮箱',
    region_code VARCHAR(20) COMMENT '所属地区编码',
    enterprise_id BIGINT COMMENT '关联企业ID(企业用户)',
    status TINYINT DEFAULT 1 COMMENT '状态:0-禁用,1-启用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username),
    INDEX idx_region_code (region_code),
    INDEX idx_enterprise_id (enterprise_id)
) COMMENT='用户表';

-- 角色表
CREATE TABLE sys_role (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    role_code VARCHAR(50) NOT NULL UNIQUE COMMENT '角色编码',
    role_name VARCHAR(100) NOT NULL COMMENT '角色名称',
    role_type VARCHAR(20) COMMENT '角色类型:system/custom',
    description VARCHAR(500) COMMENT '角色描述',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_role_code (role_code)
) COMMENT='角色表';

-- 用户角色关联表
CREATE TABLE sys_user_role (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    role_id BIGINT NOT NULL COMMENT '角色ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_role (user_id, role_id),
    INDEX idx_user_id (user_id),
    INDEX idx_role_id (role_id)
) COMMENT='用户角色关联表';

-- 权限表
CREATE TABLE sys_permission (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    permission_code VARCHAR(100) NOT NULL UNIQUE COMMENT '权限编码',
    permission_name VARCHAR(100) NOT NULL COMMENT '权限名称',
    permission_type VARCHAR(20) COMMENT '权限类型:menu/button/data',
    resource VARCHAR(100) COMMENT '资源标识',
    action VARCHAR(50) COMMENT '操作:CREATE/READ/UPDATE/DELETE',
    parent_id BIGINT DEFAULT 0 COMMENT '父权限ID',
    sort_order INT DEFAULT 0 COMMENT '排序',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_permission_code (permission_code),
    INDEX idx_parent_id (parent_id)
) COMMENT='权限表';

-- 角色权限关联表
CREATE TABLE sys_role_permission (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    role_id BIGINT NOT NULL COMMENT '角色ID',
    permission_id BIGINT NOT NULL COMMENT '权限ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_role_permission (role_id, permission_id),
    INDEX idx_role_id (role_id),
    INDEX idx_permission_id (permission_id)
) COMMENT='角色权限关联表';

-- 数据权限表
CREATE TABLE sys_data_permission (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    role_id BIGINT NOT NULL COMMENT '角色ID',
    data_type VARCHAR(50) COMMENT '数据类型:enterprise/report/analysis',
    scope VARCHAR(20) COMMENT '数据范围:self/city/province/all',
    region_code VARCHAR(20) COMMENT '地区范围',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_role_id (role_id)
) COMMENT='数据权限表';
```

### 2.2 预置角色数据

```sql
-- 系统预置角色
INSERT INTO sys_role (role_code, role_name, role_type, description) VALUES
('provincial_admin', '省级管理员', 'system', '省级系统管理员,拥有省级所有权限'),
('provincial_user', '省级普通用户', 'system', '省级普通用户,拥有数据审核和分析权限'),
('city_admin', '市级管理员', 'system', '市级管理员,拥有市级所有权限'),
('city_user', '市级普通用户', 'system', '市级普通用户,拥有数据审核权限'),
('enterprise_user', '企业用户', 'system', '企业用户,拥有数据填报和查询权限');

-- 省级管理员权限
INSERT INTO sys_permission (permission_code, permission_name, permission_type, resource, action) VALUES
('record:view', '查看备案', 'menu', 'record', 'READ'),
('record:audit', '审核备案', 'button', 'record', 'UPDATE'),
('report:view', '查看报表', 'menu', 'report', 'READ'),
('report:review', '复核报表', 'button', 'report', 'UPDATE'),
('report:submit', '上报部委', 'button', 'report', 'CREATE'),
('analysis:view', '查看分析', 'menu', 'analysis', 'READ'),
('analysis:export', '导出报表', 'button', 'analysis', 'READ'),
('system:user_manage', '用户管理', 'menu', 'system/user', 'READ'),
('system:role_manage', '角色管理', 'menu', 'system/role', 'READ'),
('system:period_manage', '时限管理', 'menu', 'system/period', 'READ'),
('system:monitor', '系统监控', 'menu', 'system/monitor', 'READ');
```

## 3. 多层级权限控制

### 3.1 角色权限矩阵

| 功能模块 | 省级管理员 | 省级普通用户 | 市级管理员 | 市级普通用户 | 企业用户 |
|---------|----------|------------|----------|------------|---------|
| 企业备案查看 | ✓(全省) | ✓(全省) | ✓(本市) | ✓(本市) | ✓(本企业) |
| 企业备案审核 | ✓ | ✓ | × | × | × |
| 数据填报 | × | × | × | × | ✓ |
| 数据初审 | × | × | ✓ | ✓ | × |
| 数据复核 | ✓ | ✓ | × | × | × |
| 报表上报 | ✓ | × | × | × | × |
| 对比分析 | ✓ | ✓ | ✓ | ✓ | × |
| 趋势分析 | ✓ | ✓ | × | × | × |
| 取样分析 | ✓ | ✓ | × | × | × |
| 报表导出 | ✓ | ✓ | × | × | × |
| 用户管理 | ✓ | × | ✓ | × | × |
| 权限管理 | ✓ | × | × | × | × |
| 时限设置 | ✓ | × | × | × | × |
| 系统监控 | ✓ | × | × | × | × |
| 密码修改 | ✓ | ✓ | ✓ | ✓ | ✓ |

### 3.2 数据权限控制

**数据范围定义:**

| 角色 | 数据范围 | 说明 |
|------|---------|------|
| 省级管理员 | 全省 | 可查看全省数据 |
| 省级普通用户 | 全省 | 可查看全省数据 |
| 市级管理员 | 本市 | 只能查看本市数据 |
| 市级普通用户 | 本市 | 只能查看本市数据 |
| 企业用户 | 本企业 | 只能查看本企业数据 |

**数据权限实现:**
```java
public class DataPermissionInterceptor implements Interceptor {
    
    @Override
    public Object intercept(Invocation invocation) throws Throwable {
        MappedStatement mappedStatement = (MappedStatement) invocation.getArgs()[0];
        Object parameter = invocation.getArgs()[1];
        
        // 获取当前用户
        User currentUser = SecurityContext.getCurrentUser();
        
        // 获取数据权限范围
        DataPermission permission = getDataPermission(currentUser);
        
        // 拼接数据权限SQL
        String sql = addDataPermissionSql(mappedStatement.getBoundSql().getSql(), permission);
        
        invocation.getArgs()[0] = modifyMappedStatement(mappedStatement, sql);
        
        return invocation.proceed();
    }
    
    private String addDataPermissionSql(String sql, DataPermission permission) {
        switch (permission.getScope()) {
            case "self":
                return sql + " AND enterprise_id = " + permission.getEnterpriseId();
            case "city":
                return sql + " AND city_code = '" + permission.getRegionCode() + "'";
            case "province":
                return sql + " AND province_code = '" + permission.getRegionCode() + "'";
            case "all":
                return sql;
            default:
                return sql;
        }
    }
}
```

### 3.3 功能权限控制

**前端权限控制:**
```vue
<template>
  <div>
    <!-- 菜单权限控制 -->
    <el-menu>
      <el-menu-item
        v-for="menu in menus"
        :key="menu.permission_code"
        v-if="hasPermission(menu.permission_code)"
      >
        {{ menu.permission_name }}
      </el-menu-item>
    </el-menu>
    
    <!-- 按钮权限控制 -->
    <el-button
      v-if="hasPermission('report:submit')"
      @click="submitReport"
    >
      上报
    </el-button>
    
    <el-button
      v-if="hasPermission('record:audit')"
      @click="auditRecord"
    >
      审核
    </el-button>
  </div>
</template>

<script>
export default {
  methods: {
    hasPermission(permissionCode) {
      return this.$store.getters.permissions.includes(permissionCode)
    }
  }
}
</script>
```

**后端权限控制:**
```java
@PreAuthorize("hasPermission('report:submit')")
@PostMapping("/api/report/submit")
public Result submitReport(@RequestBody Report report) {
    // 上报逻辑
    return Result.success();
}

@PreAuthorize("hasPermission('record:audit')")
@PostMapping("/api/record/audit")
public Result auditRecord(@RequestBody AuditRequest request) {
    // 审核逻辑
    return Result.success();
}
```

## 4. 用户关联管理

### 4.1 用户创建流程

```
省级管理员创建用户
    ↓
选择用户类型(省级/市级/企业)
    ↓
填写用户信息
    ↓
分配角色
    ↓
关联地区/企业
    ↓
发送初始密码
```

### 4.2 用户关联关系

**省级用户:**
- 关联省份编码
- 关联角色(省级管理员/省级普通用户)

**市级用户:**
- 关联省份编码
- 关联城市编码
- 关联角色(市级管理员/市级普通用户)

**企业用户:**
- 关联省份编码
- 关联城市编码
- 关联企业ID
- 关联角色(企业用户)

### 4.3 用户管理接口

**创建用户:**
```
POST /api/users
{
  "username": "user001",
  "real_name": "张三",
  "phone": "13800138000",
  "region_code": "530000",
  "city_code": "530100",
  "role_codes": ["city_user"],
  "enterprise_id": null
}
```

**分配角色:**
```
POST /api/users/{userId}/roles
{
  "role_codes": ["city_admin", "city_user"]
}
```

**查询用户权限:**
```
GET /api/users/{userId}/permissions
```

## 5. 权限验证流程

### 5.1 认证流程

```
用户登录
    ↓
验证用户名密码
    ↓
生成JWT Token
    ↓
返回Token和用户信息
    ↓
用户携带Token访问接口
    ↓
验证Token有效性
    ↓
验证权限
    ↓
返回数据
```

### 5.2 Token验证

```java
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    
    @Override
    protected void doFilterInternal(HttpServletRequest request, 
                                    HttpServletResponse response, 
                                    FilterChain filterChain) {
        String token = request.getHeader("Authorization");
        
        if (StringUtils.hasText(token) && token.startsWith("Bearer ")) {
            token = token.substring(7);
            
            if (JwtUtil.validateToken(token)) {
                String username = JwtUtil.getUsername(token);
                UserDetails userDetails = userDetailsService.loadUserByUsername(username);
                
                SecurityContext.setAuthentication(userDetails);
            }
        }
        
        filterChain.doFilter(request, response);
    }
}
```

### 5.3 权限缓存

**缓存策略:**
- 用户权限缓存到Redis
- 缓存有效期: 30分钟
- 权限变更时清除缓存

**缓存结构:**
```
Key: user:permissions:{userId}
Value: Set<permission_code>
TTL: 1800秒
```

## 6. 权限审计

### 6.1 操作日志

```sql
CREATE TABLE sys_operation_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    username VARCHAR(50) COMMENT '用户名',
    operation VARCHAR(100) COMMENT '操作',
    module VARCHAR(50) COMMENT '模块',
    method VARCHAR(200) COMMENT '请求方法',
    params TEXT COMMENT '请求参数',
    ip VARCHAR(50) COMMENT 'IP地址',
    result TINYINT COMMENT '结果:0-失败,1-成功',
    error_msg VARCHAR(500) COMMENT '错误信息',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_create_time (create_time)
) COMMENT='操作日志表';
```

### 6.2 权限变更日志

```sql
CREATE TABLE sys_permission_change_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '操作用户ID',
    target_user_id BIGINT COMMENT '目标用户ID',
    change_type VARCHAR(20) COMMENT '变更类型:ADD_ROLE/REMOVE_ROLE/ADD_PERMISSION',
    change_content TEXT COMMENT '变更内容',
    reason VARCHAR(500) COMMENT '变更原因',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_target_user_id (target_user_id)
) COMMENT='权限变更日志表';
```

## 7. 附录

### 7.1 权限编码规范

```
模块:操作
例: record:view (备案查看)
    report:submit (报表上报)
    system:user_manage (系统用户管理)
```

### 7.2 地区编码规范

| 编码 | 地区 |
|------|------|
| 530000 | 云南省 |
| 530100 | 昆明市 |
| 530300 | 曲靖市 |
| 530400 | 玉溪市 |

### 7.3 权限初始化脚本

```sql
-- 创建省级管理员用户
INSERT INTO sys_user (username, password, real_name, region_code) VALUES
('provincial_admin', '$2a$10$...', '省级管理员', '530000');

-- 关联角色
INSERT INTO sys_user_role (user_id, role_id) VALUES
(1, 1); -- provincial_admin角色
```
